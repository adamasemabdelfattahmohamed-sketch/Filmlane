# توثيق مشروع استوديو فيربيس (Firebase Studio)

هذا المستند يقدم شرحاً شاملاً لهيكل ملفات التطبيق ووظيفة كل منها. التطبيق عبارة عن محرر سيناريوهات احترافي مبني باستخدام **Next.js** و **Firebase**، ومدعوم بالذكاء الاصطناعي عبر **Genkit**.

---

## 📂 الهيكل التنظيمي للمجلدات (src)

يحتوي المجلد `src` على الكود المصدري الأساسي للتطبيق.

### 🤖 مجلد الذكاء الاصطناعي (`src/ai`)

يحتوي على إعدادات وتدفقات العمل الخاصة بالذكاء الاصطناعي.

- **`genkit.ts`**: ملف الإعداد لـ Genkit، حيث يتم تحديد المكونات الإضافية (مثل Google AI) والموديل المستخدم (Gemini 2.5 Flash).
- **`dev.ts`**: إعدادات خاصة ببيئة التطوير للذكاء الاصطناعي.
- **`flows/`**: يحتوي على "تدفقات" (Flows) الذكاء الاصطناعي:
  - **`generate-scene-ideas.ts`**: تدفق عمل لإنشاء أفكار مشاهد بناءً على موضوع أو ملخص.
  - **`auto-format-screenplay.ts`**: تدفق عمل يأخذ نصاً خاماً ويقوم بتنسيقه تلقائياً ليطابق معايير كتابة السيناريو.

### 🌐 مجلد التطبيق الرئيسي (`src/app`)

يستخدم "App Router" الخاص بـ Next.js.

- **`layout.tsx`**: التخطيط الأساسي للتطبيق، يحتوي على إعدادات الخطوط والمزودات (Providers).
- **`page.tsx`**: الصفحة الرئيسية للتطبيق، وهي تقوم باستدعاء مكون `ScreenplayEditor`.
- **`globals.css`**: ملف التنسيقات العالمي (CSS) الذي يستخدم Tailwind CSS.
- **`favicon.ico`**: أيقونة الموقع.

### 🧩 مجلد المكونات (`src/components`)

يحتوي على مكونات واجهة المستخدم (UI).

- **`screenplay-editor.tsx`**: المكون الرئيسي لمحرر السيناريو، يجمع بين شريط الأدوات ومساحة الكتابة والذكاء الاصطناعي.
- **`editor-area.tsx`**: يمثل مساحة الكتابة الفعلية للسيناريو.
- **`editor-toolbar.tsx`**: شريط الأدوات العلوي الذي يحتوي على خيارات التنسيق والخطوط.
- **`editor-header.tsx`**: رأس الصفحة الذي يحتوي على العنوان وأزرار الذكاء الاصطناعي.
- **`editor-footer.tsx`**: تذييل الصفحة الذي يعرض إحصائيات المستند (كلمات، صفحات، إلخ).
- **`theme-provider.tsx`**: لإدارة الوضع الليلي (Dark Mode).
- **`ui/`**: يحتوي على مكونات واجهة المستخدم الصغيرة والمعاد استخدامها (أزرار، مدخلات، نوافذ منبثقة، إلخ) والمنشأة بواسطة Shadcn UI.

### ⚓ مجلد الخطافات (`src/hooks`)

- **`use-mobile.tsx`**: للتحقق مما إذا كان المستخدم يتصفح من هاتف محمول.
- **`use-toast.ts`**: لإظهار رسائل التنبيه (Toasts) للمستخدم.

### 📚 مجلد المكتبات والإضافات (`src/lib`)

- **`screenplay-config.ts`**: يحتوي على إعدادات السيناريو مثل أنواع التنسيقات (مشهد، حوار، شخصية) وقوائم الخطوط والأحجام.
- **`utils.ts`**: دوال مساعدة عامة تستخدم في المشروع.
- **`logger.ts`**: لإدارة سجلات الأحداث (Logs).
- **`editor-styles.ts`**: يحتوي على أنماط CSS المخصصة لعناصر المحرر.
- **`context-memory-manager.ts`**: لإدارة ذاكرة السياق للذكاء الاصطناعي.
- **`paste-classifier.ts`**: لتصنيف النصوص عند لصقها في المحرر.
- **`placeholder-images.ts` / `.json`**: بيانات وصور تجريبية.

---

## ⚙️ ملفات الإعداد في الجذع (Root)

- **`package.json`**: يحدد الاعتماديات (Dependencies) والسكربتات الخاصة بالمشروع.
- **`next.config.ts`**: إعدادات إطار العمل Next.js.
- **`tailwind.config.ts`**: إعدادات Tailwind CSS لتنسيق الواجهة.
- **`tsconfig.json`**: إعدادات لغة TypeScript.
- **`apphosting.yaml`**: إعدادات استضافة Firebase App Hosting.
- **`components.json`**: إعدادات مكتبة Shadcn UI.
- **`README.md`**: ملف تعريفي بسيط بالمشروع.
- **`.env`**: ملف المتغيرات البيئية (مثل مفاتيح API).
- **`.gitignore`**: الملفات والمجلدات التي يجب تجاهلها في Git.
